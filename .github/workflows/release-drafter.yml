# claude-collections の単一 release-drafter ワークフロー (ADR-0006)。
#
# 役割は 2 つ:
#   (A) drafter: main マージ後に各コレクションの draft Release を更新
#   (B) autolabeler: PR open/update 時に Conventional Commits prefix から
#       label (feat/fix/refactor/docs/test/chore) を自動付与
#
# 設計判断:
#   release-drafter@v6 の action は config を default branch (= main) から
#   API 経由で読む制約がある。runtime に sed で render したファイルは API から
#   見えないため使えない。よって per-collection の config を main に commit し、
#   `scripts/regen-drafter-configs.sh` (Makefile target: `regen-drafter-configs`)
#   で template (.github/release-drafter-template.yml) から自動再生成する。
#   `verify-drafter-configs` target を CI で実行することで drift を構造的に検出。
#
# drafter (push 起点):
#   1. discover ジョブが repo top-level を走査して
#      <dir>/.claude-plugin/plugin.json を持つ dir = コレクションと判定
#   2. matrix で各コレクションごとに draft ジョブを並列起動
#   3. draft ジョブは commit 済み config (.github/release-drafter-<collection>.yml)
#      を release-drafter に渡す
#
# autolabeler (pull_request_target 起点):
#   - autolabeler ルールは collection 非依存なので discover 不要
#   - 既存の per-collection config (どれか 1 つ) を `disable-releaser: true` で
#     release-drafter に渡す → drafter 抑制、autolabeler のみ実行
#   - pull_request_target は base (main) のコードで実行されるので、
#     PR の HEAD コードを checkout せず main の commit 済み config を使う (security)
#
# 前提: リポジトリ Settings → Actions → General → Workflow permissions を
#       "Read and write permissions" にしておく (contents: write +
#       pull-requests: write 用)。
#
# workflow_dispatch は設定修正後にドラフトを再生成したい時の手動再実行用。

name: Release Drafter

on:
  push:
    branches: [main]
  pull_request_target:
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  autolabel:
    # PR 起点: autolabeler のみ実行 (drafter は disable-releaser で抑制)。
    if: github.event_name == 'pull_request_target'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Pick any per-collection config (autolabeler rules are collection-independent)
        shell: bash
        run: |
          # 任意の collection config を使う (autolabeler セクションは全 config で同一)。
          # アルファベット順で最初のものを採用 (deterministic、コレクション削除で
          # 動かなくなる事故防止のため、ls の結果から template を除外)。
          cfg=$(ls .github/release-drafter-*.yml 2>/dev/null \
            | grep -v 'release-drafter-template\.yml$' \
            | sort \
            | head -1)
          if [ -z "$cfg" ]; then
            echo "::error::No collection config found in .github/release-drafter-*.yml"
            exit 1
          fi
          echo "Using config: $cfg"
          echo "AUTOLABEL_CONFIG=$(basename "$cfg")" >> "$GITHUB_ENV"
      - name: Run autolabeler
        uses: release-drafter/release-drafter@v6
        with:
          config-name: ${{ env.AUTOLABEL_CONFIG }}
          disable-releaser: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  discover:
    # main push or 手動再実行: コレクションを発見して draft ジョブへ matrix で渡す。
    if: github.event_name != 'pull_request_target'
    runs-on: ubuntu-latest
    outputs:
      collections: ${{ steps.list.outputs.collections }}
      has_any: ${{ steps.list.outputs.has_any }}
    steps:
      - uses: actions/checkout@v4
      - id: list
        name: Discover collections
        shell: bash
        run: |
          # top-level dir で .claude-plugin/plugin.json を持つもの = コレクション
          # (root の .claude-plugin/ は marketplace.json しか持たないので除外される)
          mapfile -t found < <(for dir in */; do
            d="${dir%/}"
            if [ -f "$d/.claude-plugin/plugin.json" ]; then
              printf '%s\n' "$d"
            fi
          done)

          if [ "${#found[@]}" -eq 0 ]; then
            echo "collections=[]" >> "$GITHUB_OUTPUT"
            echo "has_any=false" >> "$GITHUB_OUTPUT"
            echo "No collections discovered."
            exit 0
          fi

          json=$(printf '%s\n' "${found[@]}" | jq -R . | jq -s -c .)
          echo "collections=$json" >> "$GITHUB_OUTPUT"
          echo "has_any=true" >> "$GITHUB_OUTPUT"
          echo "Discovered: $json"

  draft:
    needs: discover
    if: github.event_name != 'pull_request_target' && needs.discover.outputs.has_any == 'true'
    strategy:
      fail-fast: false
      matrix:
        collection: ${{ fromJson(needs.discover.outputs.collections) }}
    runs-on: ubuntu-latest
    permissions:
      contents: write     # draft Release の作成/更新
      pull-requests: read # release-drafter がノート生成のためマージ済み PR を読む
    env:
      COLLECTION: ${{ matrix.collection }}
    steps:
      - uses: actions/checkout@v4
      - name: Sanity check collection name
        shell: bash
        run: |
          # COLLECTION は discover ジョブで filesystem の dir 名から取った値で
          # 本来は trusted だが、defense-in-depth として kebab-case 系の
          # 安全な文字 ([a-z0-9][a-z0-9_-]*) のみを許可する
          if ! [[ "${COLLECTION}" =~ ^[a-z0-9][a-z0-9_-]*$ ]]; then
            echo "::error::Invalid collection name '${COLLECTION}' — must match [a-z0-9][a-z0-9_-]*"
            exit 1
          fi
          cfg=".github/release-drafter-${COLLECTION}.yml"
          if [ ! -f "$cfg" ]; then
            echo "::error::Config file not found: $cfg (run 'make regen-drafter-configs')"
            exit 1
          fi
      - name: Run release-drafter
        uses: release-drafter/release-drafter@v6
        with:
          config-name: release-drafter-${{ env.COLLECTION }}.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
